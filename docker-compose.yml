services:
  homeassistant:
    container_name: homex-ha
    image: ghcr.io/home-assistant/home-assistant:stable
    volumes:
      - ./config:/config
      # The integration lives at the repo root (HACS layout); mount it into
      # the dev config dir so HA loads it from there.
      - ./custom_components:/config/custom_components
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "8124:8123"
    restart: unless-stopped
    environment:
      - TZ=Europe/Paris

  # MQTT broker to emulate Zigbee2MQTT devices (fake test devices with actions).
  mosquitto:
    container_name: homex-mosquitto
    image: eclipse-mosquitto:2
    volumes:
      - ./mosquitto/config:/mosquitto/config
    ports:
      - "1884:1883"
    restart: unless-stopped
