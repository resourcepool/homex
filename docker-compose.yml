services:
  homeassistant:
    container_name: homex-ha
    image: ghcr.io/home-assistant/home-assistant:stable
    volumes:
      - ./config:/config
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "8124:8123"
    restart: unless-stopped
    environment:
      - TZ=Europe/Paris
